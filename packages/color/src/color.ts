interface Colors {
    primary?: string;
    secondary?: string;
    danger?: string;
    warning?: string;
}

const colors: Colors = {
    primary: "#32A6FC",
    secondary: "#52525b",
    danger: "#f74434",
    warning: "#f7c334"
};

export { colors, type Colors };
